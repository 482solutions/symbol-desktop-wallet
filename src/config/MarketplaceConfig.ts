const marketplaceServer = 'http://localhost:1823/api/v1/';

const hoursList: number[] = [6, 12, 24, 48];
console.log('Marketplace Config resolved!');
export const MarketplaceConfig = { marketplaceServer, hoursList: hoursList };
